@extends('backend.layouts.main')

@section('title')
Admin Setting
@endsection
@section('content')
<div class="body-content">
  <div class="card mb-4">
    <div class="card-body">
      <section class="forms">
        <div class="container-fluid">
          <div class="row">
            <div class="col-md-12">
              <div class="card mb-3">
                <div class="card-header"><h4>Admin Setting</h4></div>
                <div class="card-body">
                  <p class="text-muted mb-2">Showing users where <code>users.is_admin</code> is 1, 2, or 3.</p>
                  <form action="{{URL::to('setting/admin')}}" method="get" class="form-inline mb-3">
                    <input type="text" name="q" value="{{$query}}" class="form-control mr-2" placeholder="Search id, name, email">
                    <button type="submit" class="btn btn-info">Search</button>
                  </form>
                  <form action="{{URL::to('setting/admin-update')}}" method="post" class="form-inline">
                    @csrf
                    <input type="text" name="target_user" class="form-control mr-2" placeholder="User ID or email" required>
                    <select name="is_admin" class="form-control mr-2" required>
                      @foreach($accessLevels as $value => $label)
                        <option value="{{$value}}">{{$value}} - {{$label}}</option>
                      @endforeach
                    </select>
                    <input type="password" name="password" class="form-control mr-2" placeholder="New password optional">
                    <button type="submit" class="btn btn-primary">Update Access</button>
                  </form>
                  <div style="font-size:10px;text-align:center;color:#999;padding-top:12px;">Powerd by JAMBOai</div>
                </div>
              </div>
              <div class="card">
                <div class="card-header"><h4>Admin / Country Admin / Subadmin Users</h4></div>
                <div class="table-responsive">
                  <table class="table display table-bordered table-striped table-hover basic">
                    <thead>
                      <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Access</th>
                        <th>Change</th>
                      </tr>
                    </thead>
                    <tbody>
                      @foreach($users as $user)
                      <tr>
                        <td>{{$user->id}}</td>
                        <td>{{$user->name}}</td>
                        <td>{{$user->email}}</td>
                        <td>{{$user->is_admin}} - {{ $accessLevels[(int)$user->is_admin] ?? 'Unknown' }}</td>
                        <td>
                          <form action="{{URL::to('setting/admin-update')}}" method="post" class="form-inline">
                            @csrf
                            <input type="hidden" name="target_user" value="{{$user->id}}">
                            <select name="is_admin" class="form-control mr-2" required>
                              @foreach($accessLevels as $value => $label)
                                <option value="{{$value}}" @if((int)$user->is_admin === (int)$value) selected @endif>{{$value}} - {{$label}}</option>
                              @endforeach
                            </select>
                            <input type="password" name="password" class="form-control mr-2" placeholder="New password optional">
                            <button type="submit" class="btn btn-sm btn-primary">Save</button>
                          </form>
                        </td>
                      </tr>
                      @endforeach
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>
</div>
@endsection
